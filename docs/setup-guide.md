# Setup Guide

> Manual setup steps for Joe. Agents flesh this out as they prepare each phase.
> Most of this maps to `HUMAN-TODO.md` items.

## Status

Stub. Will be filled in by **TASK-003** (AltServer/AltStore install walkthrough).
Other sections added when their phase becomes active.

## Sections to fill in

- One-time JARVIS setup (iTunes, AltServer, autostart)
- One-time iPhone setup per device (USB pair, AltStore install, trust profile)
- Adding the AltStore source for PhotoSwiper (URL + screenshots)
- First-run on the phone (granting Photos permission, entering Immich URL + API key)
- Generating an Immich API key (Immich web UI walkthrough)
- Maintenance: what to do if the app icon grays out
- Maintenance: what to do if a CI build fails
