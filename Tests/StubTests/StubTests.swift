import XCTest

/// Placeholder so the test target has at least one source file and compiles
/// before the real test files land in TASK-023 (PhotoFetcher), TASK-033
/// (UndoStack), and TASK-044 (ImmichClient). Safe to delete once any real
/// tests exist.
final class StubTests: XCTestCase {
    func testStubExists() {
        XCTAssertTrue(true)
    }
}
